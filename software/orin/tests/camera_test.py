#!/usr/bin/env python3
"""
camera_test.py — STUB. Generate after camera.py.
Quick manual check:
  python3 -c "import cv2; cap=cv2.VideoCapture(0); ok,f=cap.read(); print('OK' if ok else 'FAIL', f.shape if ok else '')"
"""
raise NotImplementedError("Generate after camera.py.")
